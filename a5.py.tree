#----------------------------------------------------------------------------------------------------------------
Module(body=[
	Assign(targets=[
		Name(id='params', 
			ctx=Store())], 
		value=Call(
			func=Attribute(
				value=Name(id='parametertools', 
					ctx=Load()), attr='parse_dma_params', 
				ctx=Load()), args=[], keywords=[], starargs=None, kwargs=None))])
#----------------------------------------------------------------------------------------------------------------
