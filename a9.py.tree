#----------------------------------------------------------------------------------------------------------------
Module(body=[
	Assign(targets=[
		Name(id='some_var', 
			ctx=Store())], 
		value=Subscript(
			value=Name(id='params', 
				ctx=Load()), 
			slice=Index(
				value=Str(s='Injectable Code')), 
			ctx=Load()))])
#----------------------------------------------------------------------------------------------------------------
